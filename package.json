{
  "name": "cpigjs",
  "version": "0.1.0",
  "description": "Conditional Predicate Implication Graph",
  "type": "module",
  "scripts": {
    "test": "npx tsc && npx mocha scripts/test.js",
    "buildWeb": "npx tsc && node scripts/templateFill.js -t template.html -c fairDiv/htmlContext.json -o fairDiv/index.html",
    "clean": "rm -f cpigjs/*.js cpigjs/*.js.map fairDiv/index.html"
  },
  "author": "Eklavya Sharma",
  "license": "MIT",
  "dependencies": {
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "mocha": "^11.0.1"
  }
}
